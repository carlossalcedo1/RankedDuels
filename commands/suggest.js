const Discord = require('discord.js');
module.exports = {
  name: 'suggest',
  run (message, args) {
   const Embed = new Discord.messageEmbed();
    .setTitle('TEST');
    .setAuthor('HELLO'); 
  }
}